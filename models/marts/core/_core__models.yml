version: 2

models:
  - name: dim_customers
    columns:
    - name: customer_id
      tests:
        - unique
        - not_null
        - relationships:
            to: ref('stg_customers')
            field: customer_id
  - name: fct_orders
    columns:
    - name: order_id
      tests:
        - unique
        - relationships:
            to: ref('stg_orders')
            field: order_id
    - name: customer_id
      tests:
        - not_null
  - name: orders_by_customer_9
    description: Number of orders placed by each customer
    columns:
    - name: customer_id
      tests:
        - unique
        - not_null
